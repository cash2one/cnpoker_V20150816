#include "LoginServer.h"

int main(int argc, char ** argv) 
{
	#if 0
	g_LoginServer = new g_LoginServer;
	g_LoginServer->Init();
	
	while(true) {
		Sleep(100);
		g_LoginServer->Update(0);
	}
	#endif
	
	return 0;
}